@model IEnumerable<CRMFacilitoInicial.Models.Contacto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreCompleto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Principal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Principal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContactoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContactoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContactoId })
        </td>
    </tr>
}

</table>
