@model IEnumerable<CRMFacilitoInicial.Models.Telefono>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumeroTelefonico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Principal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroTelefonico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Principal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TelefonoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TelefonoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TelefonoId })
        </td>
    </tr>
}

</table>
